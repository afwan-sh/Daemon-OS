package com.daemonos.bootSystem;

import com.daemonos.userdata.LoginData;

public class Boot {
    public static void main(String[] args) {
        LoginData log = new LoginData();
        boolean run = true;
        while (run) {
            if (log.checkData().equals("")) {

            }
        }
    }
}
